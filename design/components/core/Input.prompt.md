Single-line text field on warm latte surface, with optional leading icon. Search bars, email capture, promo codes.

```jsx
<Input icon="search" placeholder="Kahve, ekipman ara…" />
<Input icon="mail" type="email" placeholder="eposta@adresiniz.com" />
```

No visible border focus ring — the latte fill + hairline carry it. Pair with a `Button` for submit rows.
